package com.hjoven.prueba.shared.utils;

import java.util.Arrays;

public class GlobalConstants {
    public static final String[] EXCLUDED_FIELDS = Arrays.asList("id", "personaId").toArray(new String[0]);
}
